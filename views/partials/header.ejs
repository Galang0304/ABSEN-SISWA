<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>SDN 203 BONTOMACINNA</title>
    
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    
    <!-- Font Awesome -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css" rel="stylesheet">
    
    <!-- Custom CSS -->
    <style>
        :root {
            --primary: #6f42c1;
            --primary-dark: #5a32a3;
            --primary-light: #845ecd;
            --secondary-color: #e9ecef;
        }

        body {
            background-color: #f8f9fa;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }
        
        .sidebar {
            position: fixed;
            top: 0;
            bottom: 0;
            left: 0;
            z-index: 100;
            padding: 0;
            background: var(--primary);
            color: white;
            width: 240px;
            transition: all 0.3s;
        }

        .sidebar .nav-link {
            color: rgba(255,255,255,.9);
            padding: 0.8rem 1.5rem;
            font-size: 0.95rem;
            display: flex;
            align-items: center;
            transition: all 0.3s;
        }

        .sidebar .nav-link:hover,
        .sidebar .nav-link.active {
            background: var(--primary-dark);
            color: white;
        }

        .sidebar .nav-link i {
            margin-right: 10px;
            width: 20px;
            font-size: 1.1rem;
        }

        main {
            margin-left: 240px;
            padding: 2rem;
            background: #fff;
            min-height: 100vh;
        }

        .top-header {
            background: var(--primary);
            padding: 1rem 2rem;
            display: flex;
            align-items: center;
            justify-content: space-between;
            position: fixed;
            top: 0;
            right: 0;
            left: 240px;
            z-index: 99;
            height: 64px;
        }

        .brand-container {
            display: flex;
            align-items: center;
            gap: 1rem;
        }

        .brand-logo {
            width: 40px;
            height: 40px;
            border-radius: 50%;
            background: #fff;
            padding: 5px;
        }

        .brand-text {
            color: white;
            font-size: 1.2rem;
            font-weight: 600;
            margin: 0;
        }

        .nav-menu {
            display: flex;
            gap: 1rem;
        }

        .nav-menu a {
            color: rgba(255,255,255,.9);
            text-decoration: none;
            padding: 0.5rem 1rem;
            border-radius: 5px;
            transition: all 0.3s;
        }

        .nav-menu a:hover {
            background: var(--primary-dark);
            color: white;
        }

        .stat-card {
            background: linear-gradient(45deg, var(--primary), var(--primary-light));
            color: white;
            border-radius: 15px;
            padding: 1.5rem;
            margin-bottom: 1rem;
            box-shadow: 0 4px 6px rgba(0,0,0,0.1);
        }

        .stat-card h3 {
            font-size: 2rem;
            margin: 0;
            font-weight: 600;
        }

        .stat-card p {
            margin: 0;
            opacity: 0.9;
        }

        .stat-icon {
            font-size: 2rem;
            opacity: 0.5;
        }

        .table {
            background: white;
            border-radius: 10px;
            overflow: hidden;
            box-shadow: 0 2px 4px rgba(0,0,0,.05);
        }

        .table th {
            background: #f8f9fa;
            font-weight: 600;
            border-bottom: 2px solid #dee2e6;
        }

        .btn-primary {
            background: var(--primary);
            border-color: var(--primary);
        }

        .btn-primary:hover {
            background: var(--primary-dark);
            border-color: var(--primary-dark);
        }

        @media (max-width: 768px) {
            .sidebar {
                transform: translateX(-100%);
            }

            .sidebar.show {
                transform: translateX(0);
            }

            main {
                margin-left: 0;
            }

            .top-header {
                left: 0;
            }
        }
    </style>
</head>
<body>
    <div class="top-header">
        <div class="brand-container">
            <img src="/images/logo.png" alt="Logo Sekolah" class="brand-logo">
            <h1 class="brand-text">SDN 203 BONTOMACINNA</h1>
        </div>
        <div class="nav-menu">
            <a href="/profil">Profil</a>
            <a href="/statistik">Statistik</a>
            <a href="/kontak">Kontak</a>
            <a href="/logout">
                <i class="fas fa-sign-out-alt"></i>
                Keluar
            </a>
        </div>
    </div>
</body>
</html> 